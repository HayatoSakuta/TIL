---
title: usesとrunの違い
---

- GitHubActionsのワークフロー(yml)内では、`uses`や`run`で実行内容を指定できる。
- `uses`
  - 他の人が作成したActionsを実行する際に使う。
  - 例: 
    - `uses: actions/checkout@v4`
    - `actions/setup-node@v4`
- `run`
  - bashコマンドを実行する際に使う。
  - 例: 
    - `npm install`
    - `npm run docs:build`
