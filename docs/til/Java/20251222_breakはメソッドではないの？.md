---
title: "breakはメソッドではないの？"
date: 2025-12-22
status: published
---

# 20251222_breakはメソッドではないの？

繰り返し処理を抜けるために使用される `break`は、メソッドではない。
そのため、 `break();`ではなく `break;` と記述する。
`break`はメソッドではなく、for文やif文などと同様に、Javaの構文の一つである。